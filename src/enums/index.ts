export enum RoleType {
  KDA = 1,
  User = 2,
  CityCourt = 3,
  DCO = 4,
  RD = 5,
}

export enum SortDirection {
  ASC = 0,
  DESC = 1,
}

export enum Gender {
  Male = 1,
  Female = 2,
  Other = 3,
}

export enum MaritalStatus {
  Single = 1,
  Married = 2,
}

export enum Schemes {
  Gulshan = 1,
  Jauhar = 2,
  Gulberg = 3,
  SoldierMarket = 4,
}

export enum PlotCategory {
  A = 1,
  B = 2,
  C = 3,
  D = 4,
}

export enum PlotNature {
  Leased = 1,
  NonLeased = 2,
}

export enum TransactionType {
  Mutation = 1,
  Transfer = 2,
  Inheritance = 3,
}

export enum SaleDeedStatus {
  WaitingBuyer = 1,
  ChallanPendingRD = 2,
  WaitingRegistrarApproval = 3,
  MeetingSchedule = 4,
  CityCourt = 5,
  Transferred = 6,
  PaymentPending = 7,
  PaymentVerificationPending = 8,
  PaymentVerified = 9,
  ChallanPendingKDA = 10,
}
export enum IpStatus {
  Published = "Published",
  AppliedForPatent = "Applied For Patent",
  InActive = "In Active",
  Draft = "Draft",
}
